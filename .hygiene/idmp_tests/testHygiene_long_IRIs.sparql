##
# banner No IRI should be longer than 255 chars.


SELECT DISTINCT ?error ?resource
WHERE
{
  ?resource ?property ?value .
  FILTER (STRLEN(STR(?resource)) > 255)
  FILTER regex(str(?resource), "${NAMESPACE_REGEX}")
  BIND (concat("PRODERROR: IRI ", STR(?resource), " is longer than 255 chars.") AS ?error)
}
